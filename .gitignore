# IDE & OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# PHP & Composer
vendor/
.php-cs-fixer.cache
.phpunit.result.cache
.pestphp-laravel-unit-test-result-cache
composer.lock

# Инструменты качества
.rector_cache/
build/
pint.json.bak

# VitePress (документация)
node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/

# Тесты и логи
storage/*.log
.env
tests/temp/

# AI / dev tooling — трекается (видимость правок агентов). Machine-specific конфиги
# (абсолютные пути) исключены точечно; рядом — синхронный *.example с плейсхолдерами
# для восстановления (см. project-tooling-runbook → «Гигиена machine-specific конфигов»).
.claude/settings.local.json
.claude/settings.local.json.bak
.claude/.maind-feature-log.last
.mcp.json
*.bak
